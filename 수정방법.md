# 안내 페이지 고치는 방법

**https://beyond-sasong.pages.dev**

전화번호가 바뀌거나 블로그 글을 새로 연결하고 싶을 때, 개발자 없이 직접
고칠 수 있습니다. 프로그램을 깔 필요도 없고 이 화면에서 다 됩니다.

**고치고 저장하면 1~2분 뒤 실제 페이지에 자동으로 반영됩니다.**

---

## 고치는 순서

### 1. 편집 화면을 연다

아래 주소를 누르면 바로 열립니다.

**https://github.com/beyondtopenglab/beyond-sasong/edit/main/index.html**

> 손으로 찾아가려면 — 저장소 첫 화면에서 `index.html` 을 누른 뒤,
> 오른쪽 위 **연필 아이콘 ✏️** 을 누릅니다.

### 2. 고칠 곳을 찾는다

`Ctrl + F` (맥은 `Cmd + F`) 를 눌러 찾고 싶은 버튼 이름을 칩니다.
예를 들어 `전화로 문의하기` 라고 치면 그 자리로 갑니다.

### 3. 따옴표 안쪽만 바꾼다

```html
<a class="plain" href="여기만 바꿉니다">
```

**`href="` 와 `">` 는 절대 지우지 마세요.** 그 사이의 주소만 바꿉니다.

### 4. 저장한다

오른쪽 위 초록색 **Commit changes...** → 창이 뜨면 다시 **Commit changes**

무엇을 바꿨는지 한 줄 적어 두면 나중에 찾기 쉽습니다 (예: `전화번호 변경`).

### 5. 반영됐는지 본다

저장소 첫 화면에서 맨 위 줄 오른쪽을 보세요.

| 표시 | 뜻 |
|---|---|
| 🟡 노란 점 | 반영 중 — 조금 기다립니다 |
| ✅ 초록 체크 | 완료. 페이지에 반영됐습니다 |
| ❌ 빨간 X | 실패. 뭔가 잘못 지웠을 수 있습니다 |

---

## 어디에 무엇이 들어가나

`index.html` 안의 위치입니다. 줄 번호는 수정하면 조금씩 밀립니다 —
**`Ctrl + F` 로 버튼 이름을 찾는 편이 확실합니다.**

| 화면의 버튼 | 지금 연결된 곳 | 넣을 주소 |
|---|---|---|
| 카카오톡으로 상담 신청하기 | `pf.kakao.com/_TjUxln` | 카카오톡 채널 주소 |
| **비욘드 교육과정 살펴보기** | **비어 있음** | 교육과정 소개 블로그 글 |
| **사송캠퍼스 수업 이야기** | **비어 있음** | 수업 후기 블로그 글 |
| 학원 위치 · 주차 안내 | `naver.me/5MV7BFZw` | 네이버 지도 장소 주소 |
| 전화로 문의하기 | `tel:055-388-2705` | `tel:` 뒤에 번호 |
| 인스타그램 | `instagram.com/beyondenglish_sasong/` | |
| 네이버 블로그 | `m.blog.naver.com/beyondenglish_sasong?tab=1` | |

아직 비어 있는 두 곳은 `href="#"` 라고 되어 있습니다.
`Ctrl + F` 로 `href="#"` 를 찾으면 바로 나옵니다.

---

## 자주 하는 일

### 전화번호 바꾸기

`전화로 문의하기` 를 찾으면 **두 군데**가 나옵니다. 둘 다 바꿔야 합니다.

```html
<a class="plain" href="tel:055-388-2705">      ← 눌렀을 때 걸리는 번호
      <b class="tel">055-388-2705</b>         ← 화면에 보이는 번호
```

`tel:` 은 지우지 마세요. 그게 있어야 휴대폰에서 눌렀을 때 바로 걸립니다.

### 버튼을 아예 없애기

쓰지 않는 버튼은 지우는 편이 낫습니다. **눌러도 아무 일이 없는 버튼은
없느니만 못합니다.**

`<a` 로 시작하는 줄부터 `</a>` 로 끝나는 줄까지 통째로 지웁니다.
예를 들어 «사송캠퍼스 수업 이야기» 를 없애려면 이 덩어리 전체를 지웁니다.

```html
    <!-- 수업 후기·소식 블로그 주소 -->
    <a class="plain" href="#">
      <svg ... </svg>
      <span>사송캠퍼스 수업 이야기</span>
      <svg class="chev" ... </svg>
    </a>
```

### 버튼 이름 바꾸기

`<span>` 과 `</span>` 사이의 글자를 바꿉니다.

```html
<span>전화로 문의하기</span>
```

### 위쪽 문구 바꾸기

```html
<span>사송캠퍼스</span>                                    ← 캠퍼스 이름
<p class="claim">결과가 다른 영어 · <em>Learn Beyond.</em></p>  ← 빨간 한 줄
<p class="sub">Phonics부터 수능까지 이어지는 영어 성장 로드맵</p>  ← 회색 설명
```

### 등록번호 바꾸기

맨 아래쪽입니다.

```html
<p class="legal">양산교육지원청 등록 제2285호 · 경남 양산시</p>
```

---

## 잘못 고쳤을 때

**모든 변경 이력이 남아 있어서 언제든 되돌릴 수 있습니다.** 겁내지 말고
고치셔도 됩니다.

1. 저장소 첫 화면에서 **Commits** (시계 모양 옆 숫자) 를 누른다
2. 되돌리고 싶은 시점의 항목을 누른다
3. 오른쪽 위 **⋯ → Revert**

또는 개발자에게 «○○ 고친 걸 되돌려 주세요» 라고 말하면 한 줄로 됩니다.

---

## 이건 건드리지 않아도 됩니다

- **Cloudflare** — 한 번 연결해 뒀습니다. 이후로는 GitHub 만 보면 됩니다
- **`tools/` 폴더** — 미리보기 그림을 만들 때 쓰는 것으로, 페이지와 무관합니다
- **`<style>` 부터 `</style>` 까지** — 색과 모양을 정하는 부분입니다.
  건드리면 화면이 깨질 수 있으니 개발자에게 맡기세요

---

## 주소가 바뀌면 (도메인을 붙이면)

`beyond-sasong.pages.dev` 대신 학원 도메인을 붙이는 경우, 파일 위쪽의
두 줄도 함께 바꿔야 **카카오톡에 링크를 붙였을 때 미리보기 카드**가
계속 뜹니다.

```html
<meta property="og:url" content="https://beyond-sasong.pages.dev/">
<meta property="og:image" content="https://beyond-sasong.pages.dev/assets/og.png">
```

이건 개발자에게 맡기는 편이 안전합니다.
